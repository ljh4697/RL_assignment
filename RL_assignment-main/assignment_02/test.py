import numpy as np
import random
import math
import matplotlib.pyplot as plt


#state = np.array([0])
state = np.zeros(16)
#state = np.reshape( state, [1, 16])
state = np.array([state])

print(state)


